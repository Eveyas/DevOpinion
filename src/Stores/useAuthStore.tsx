// import create from 'zustand';

// interface AuthState {
//   role: 'admin' | 'moderator' | 'user' | 'guest'; // Define los roles disponibles
//   setRole: (role: 'admin' | 'moderator' | 'user' | 'guest') => void;
// }

// const useAuthStore = create<AuthState>((set) => ({
//   role: 'guest', // Rol inicial
//   setRole: (role) => set({ role }),
// }));

// export default useAuthStore;